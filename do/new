#!/usr/bin/env -S execlineb -WS 1

# Create a new page for the website from the Markdown sample page using Vim.

importas fname 1
importas -i edit EDITOR
backtick -E uuid { pipeline { uuidgen } tr A-Z a-z }
$edit "ssg/sample.md" +"f ${1}" +"2s/UUID/${uuid}/"
